import 'package:flutter/material.dart';

class IglesiaPage extends StatelessWidget {
  const IglesiaPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Acerca de nosotros'),
      ),
      
    );
  }
}